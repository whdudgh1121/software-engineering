.hero {
    background-color: rgb(0, 173, 173); /* 배경색 */
    font-size: 30px;
    padding: 10px; /* 패딩 */
    display: flex; /* 플렉스 레이아웃 사용 */
    align-items: center; /* 수직 정렬 */
    justify-content: center; /* 요소 간 간격 설정 */
}
.display-4{
  height: 75px;
  padding: 1rem;
  color: white;
  background: rgb(0, 173, 173);
  font-weight: bold;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.select-bar{
  display: flex;
  color: white;
  font-size: 30px;
}


/* 로그인창 */



.main-content {
    display: flex; /* 플렉스 레이아웃 사용 */
}

.content-area {
    width: 75%; /* 메인 콘텐츠 영역의 너비 설정 */
    padding: 20px; /* 패딩 */
}

.hero-container {
    display: flex;
    flex-direction: column; /* 컨테이너 내부 요소를 세로로 정렬 */
}

.login-button {
    background-color: white; /* 버튼 배경색 */
    color: #333333; /* 텍스트 색상 */
    border: 1px solid #333333; /* 테두리 색상 */
    border-radius: 5px; /* 모서리 둥글게 */
    padding: 5px 10px; /* 버튼 패딩 */
    cursor: pointer; /* 마우스 오버 시 포인터 표시 */
    font-size: 0.9em; /* 텍스트 크기 */
    float: right;
}

.login-button:hover {
    background-color: #f5f5f5; /* 마우스 오버 시 배경색 변경 */
}
.logout-button {
    background-color: gray;
    color: white;
    border: none;
    padding: 10px;
    cursor:pointer;
}
.logout-button:hover {
    background-color: red;
    color: white;
    border: none;
    padding: 10px;
    cursor:pointer;
}
.modal {
    display: none; /* 기본적으로 숨김 */
    position: fixed; /* 화면 중앙에 위치 */
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 1000;
    background-color: white;
    padding: 20px;
    border: 1px solid #ccc;
    box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
}

/* 모달 배경 (모달 바깥쪽 클릭 시 모달 닫기용) */
.modal-backdrop {
    display: none; /* 기본적으로 숨김 */
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* 반투명 배경 */
    z-index: 999;
}
/* 노멀라이즈 시작 */
body, ul, li {
    margin: 0;
    padding: 0;
    list-style: none;   /* 해당 태그의 list-style을 none으로 하는 것으로 ●을 제거한다 */
  }
  
